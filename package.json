{
  "name": "smallworld-engine",
  "version": "0.8.22",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write .",
    "start": "npm run build && npx serve ."
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "serve": "^14.0.0",
    "prettier": "^3.2.0",
    "@webgpu/types": "^0.1.40"
  }
}
